FROM alpine:3.7
COPY ./dist/ .
RUN apk add --no-cache ca-certificates
ARG PROJECT_NAME=rtr_api
ENV PROJECT_NAME=$PROJECT_NAME
ENV LOG_FACILITY=$PROJECT_NAME
ENV SERVER_HOST 0.0.0.0
ENV SERVER_PORT 8080
CMD ./$PROJECT_NAME 2>&1 | tee -a out.log
EXPOSE 8080
